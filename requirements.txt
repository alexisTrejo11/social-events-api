
# Core
django==5.1.6
psycopg2-binary==2.9.10          # PostgreSQL driver

# REST API
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1   # JWT auth
drf-spectacular==0.27.2                # OpenAPI / Swagger docs
django-cors-headers==4.4.0             # CORS headers

# Authentication
django-allauth==65.3.0            # Email verification, social login (future-ready)

# Image handling
Pillow==11.1.0                    # Required by ImageField

# Environment & configuration
django-environ==0.11.2            # .env file support

# Storage (media files)
django-storages==1.14.4           # S3 / GCS backend for avatars, covers, logos
boto3==1.37.13                    # AWS S3 (used by django-storages)

# Task queue (notifications, reminders, recurrence generation)
celery==5.4.0
django-celery-beat==2.7.0         # Periodic tasks (event reminders)
redis==5.2.1                      # Celery broker + cache backend

# Caching
django-redis==5.4.0

# Filtering & search
django-filter==24.3               # DRF query param filtering

# Production server
gunicorn==23.0.0
whitenoise==6.8.2

# Dev & tooling
django-debug-toolbar==4.4.6
ipython==8.32.0

# Testing
pytest==8.3.5
pytest-django==4.9.0
factory-boy==3.3.1                # Model factories for tests
faker==33.3.1                     # Fake data for factories